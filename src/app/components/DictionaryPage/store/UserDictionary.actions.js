import { createAction } from '../../../store/create-action';

export const setUserDictionary = createAction('[User Dictionary] Set User Dictionary');
export const updateUserWord = createAction('[User Dictionary] Update User Word');
export const addUserWord = createAction('[User Dictionary] Add new User Word');
