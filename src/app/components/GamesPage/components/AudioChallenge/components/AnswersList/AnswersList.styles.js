import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  listOfAnswers: {
    display: 'flex',
    marginTop: 50,
    listStyleType: 'none',
    fontSize: '20px',
    lineHeight: '45px',
  },
});
