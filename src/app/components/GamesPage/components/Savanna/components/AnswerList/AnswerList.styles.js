import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  answerList: {
    display: 'flex',
    flexWrap: 'wrap',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '18px',
    lineHeight: '45px',
    marginLeft: '50px',
    height: '200px',
  },
});
