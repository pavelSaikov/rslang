import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  symbol: {
    marginLeft: '30px',
    display: 'block',
    width: '30px',
    height: '30px',
    fontSize: '30px',
    color: 'black',
    cursor: 'pointer',
  },
});
